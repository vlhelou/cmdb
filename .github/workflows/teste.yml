name: teset
on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: cria Imagem
      run: | 
        echo inicia
        echo ${{github.ref_name}}
    
    

